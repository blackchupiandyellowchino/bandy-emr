import time
import sys


class Tarjeta:
	def __init__(self):
		self.arg = arg
	
	def payTicket (self):

# saldo - costo


	def reload (self,toload):
		if self.toload == 196:
			self.guita = self.guita + 230
		elif self.toload == 368:
			self.guita = self.guita + 460
		else
			self.guita = self.guita + self.toload


	def money (self):
		return self.guita


	def doneTravels (self):
		# if self.bondi_ant == false and Bondi_anterior != Bondi_actual

		# Don't know como representar los bondis



class TarjetaComun (Tarjeta):
	def __init__ (self):


class TarjetaMedioBoleto (Tarjeta):
	def __init__ (self):

# Sólo válidas de 6 a 24 hs, todos los días (incluyendo sábados y domingos). Meter un if en money()

class Bondis:
	def __init__ (self, empresa, linea, interno):
		self.emp = empresa
		self.line = linea
		self.int = interno

# Empresa, línea, interno 


class Viaje:
	def __init__ (self):
		self.cant_viajes = 0
		list_viajes = []


# bondi, horario, monto del pasaje